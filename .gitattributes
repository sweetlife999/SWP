# Shell scripts run inside Linux containers, so they must stay LF even when
# checked out on Windows (git autocrlf would otherwise inject CRLF and break
# the shebang, e.g. `/usr/local/bin/run-migrations.sh: no such file or directory`).
*.sh text eol=lf
