# Copy to .env (git-ignored) before running docker compose.
# These are local-development defaults — use real secrets in production.

POSTGRES_DB=su_portal
POSTGRES_USER=su
POSTGRES_PASSWORD=su_dev_password

# Connection string for a backend service running on the host:
#   postgres://su:su_dev_password@127.0.0.1:5432/su_portal
